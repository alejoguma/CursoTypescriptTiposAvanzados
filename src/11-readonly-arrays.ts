const numbers: ReadonlyArray<number> = [1,2,3,3];
// numbers.push(1); No se podrian usar
// numbers.pop();
// numbers.unshift(1);
numbers.filter(() => {});
numbers.reduce(() => 0);
numbers.map(() => 0);



